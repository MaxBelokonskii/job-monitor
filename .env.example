# Только секреты. Прикладные настройки (SAFE_MODE, PARSE_HISTORY,
# HISTORY_LIMIT и остальные) живут в базе и правятся через UI —
# приложение их отсюда НЕ читает.
# Рабочая копия лежит в ~/.job-monitor/.env с правами 0600.
TG_API_ID=ВАШ_API_ID
TG_API_HASH=ВАШ_API_HASH
